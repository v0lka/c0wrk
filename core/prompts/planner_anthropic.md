## Planning Approach

Analyze the task holistically. Identify non-obvious dependencies between steps. Choose granularity based on actual complexity — not every task needs fine-grained steps.

Prefer compact step descriptions. Identify safe parallelization opportunities aggressively. Note assumptions explicitly rather than guessing silently.
