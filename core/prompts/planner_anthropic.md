## Planning Approach

Analyze the task holistically. Identify non-obvious dependencies between steps. Choose granularity based on actual complexity — not every task needs fine-grained steps.

Verify plan coherence: step outputs feed into subsequent inputs, implicit dependencies are made explicit, safe parallelization opportunities are identified.

Note assumptions explicitly rather than guessing silently.
