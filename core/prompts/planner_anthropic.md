## Planning Approach

Analyze the task holistically. Identify non-obvious dependencies. Choose granularity based on actual complexity.

Verify plan coherence: step outputs feed into subsequent inputs, implicit dependencies are explicit, safe parallelization is identified.

Note assumptions explicitly rather than guessing silently.
