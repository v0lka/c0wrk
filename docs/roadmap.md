# c0wrk: Долгосрочный роадмап развития

## Текущее состояние (базовая линия)

c0wrk -- десктопный AI coding-агент продакшн-уровня с многослойной архитектурой (desktop -> backend -> core -> sdk), трёхфазной оркестрацией (маршрутизация -> планирование -> выполнение с рефлексией), 5 LLM-провайдерами, 15+ встроенными инструментами, MCP-шлюзом, ONNX-based векторным поиском, 4 стратегиями управления контекстом (sliding window, summarization, hierarchical, tool output pruning), полноценным PTY-терминалом, DAG-based параллельным выполнением с субагентами-горутинами и развитым React 19-фронтендом с 25+ типами событий и 12 Zustand-хранилищами.

Агентный цикл содержит 5+ circuit breaker'ов, оверлеи промптов под семейства моделей и профили агентов-специалистов (researcher, coder, tester, executor). Реализовано: персистентность фактов в SQLite (уровень задачи), автоматическая инъекция RAG-хинтов перед маршрутизацией/планированием, кэширование промптов для Anthropic (CacheBreak + ephemeral cache control), адаптивный выбор стратегии компактизации по домену. Модель безопасности включает политики инструментов, LLM-судью и regex-блоклисты.

Анализ ниже структурирован в виде **треков** (параллельных направлений работ). Внутри каждого трека элементы расположены в рекомендуемом порядке реализации. Для каждого элемента указан текущий статус реализации, исследовательский компонент и результат реализации.

---

## Трек 1: Персистентная память и граф знаний

**Текущее состояние**: Факты персистентны на уровне задачи (таблица `task_facts` в SQLite, `PersistentBlackboard.StoreFact()` сохраняет, `RestoreBlackboard()` восстанавливает). Однако нет кросс-сессионного/кросс-проектного поиска фактов, нет базы знаний на уровне проекта, нет понимания архитектуры через граф.

### 1.1 Расширение области видимости фактов до уровня проекта

> **Статус**: частично реализовано -- персистентность фактов работает, но только в рамках задачи (`task_id`), без project-wide поиска.

- **Исследование**: Оценить FTS5 виртуальную таблицу для полнотекстового поиска по фактам. Бенчмарк FTS5 на типичных объёмах (1K-10K фактов на проект). Оценить миграцию с текущей схемы `task_facts` (JSON-блоб) на нормализованную таблицу с отдельными записями.
- **Реализация**: Расширить таблицу `task_facts` или создать нормализованную `project_facts` (`id, project_id, session_id, task_id, keywords, content, author, created_at`). Добавить `SearchProjectFacts()` для поиска по всем задачам/сессиям в пределах одного проекта. Автоматически инжектировать релевантные факты из предыдущих сессий при старте новой задачи.

### 1.2 Проектная база знаний (структурированные заметки)

> **Статус**: отсутствует.

- **Исследование**: Изучить паттерн «structured note-taking» от Anthropic (агенты ведут `CLAUDE.md`-подобные файлы). Оценить, использовать файловую систему (markdown-файлы) или записи в БД.
- **Реализация**: Добавить инструмент `project_notes` (чтение/запись/поиск по markdown-заметкам, привязанным к проекту, в `~/.c0wrk/projects/{id}/notes/`). Автоматически инжектировать релевантные заметки в системный промпт оркестратора через семантический поиск. Агент пишет ADR (Architecture Decision Records), архитектурные решения, изученные паттерны.

### 1.3 Интеграция графа знаний

> **Статус**: отсутствует -- нет tree-sitter, нет AST-based анализа, нет графовых запросов в кодовой базе.

- **Исследование**: Оценить легковесные встроенные графовые хранилища (например, Cayley или таблица отношений в SQLite). Профилировать компромисс графовых запросов vs. плоский FTS5.
- **Реализация**: Построить модуль `CodeGraph`, индексирующий: функция -> вызовы, тип -> реализации, файл -> импорты, модуль -> зависимости. Заполнять из AST через tree-sitter (Go, TS, Python первоначально). Предоставить инструмент `query_codebase_graph` для запросов агента типа «что вызывает функцию X?» или «что зависит от модуля Y?».

### 1.4 Кросс-проектное обучение

> **Статус**: отсутствует -- факты привязаны к `task_id`, таблица `global_insights` не существует.

- **Исследование**: Какие знания переносятся между проектами? Изучить паттерны: стратегии восстановления после ошибок, паттерны использования инструментов, типичные архитектурные решения.
- **Реализация**: Добавить таблицу `global_insights` (не привязанную к проекту). Агент сохраняет обобщаемые выводы. На новом проекте инжектировать top-N релевантных инсайтов в системный промпт.

### 1.5 Подгрузка файлов в Blackboard

> **Статус**: отсутствует -- Blackboard хранит факты и файловые изменения по шагам (`SetStepFileChanges()`), но не предоставляет механизма загрузки содержимого файлов для кросс-шагового доступа.

- **Исследование**: Оценить объём данных, целесообразный для хранения в Blackboard (целые файлы vs. фрагменты vs. только метаданные). Профилировать влияние на потребление памяти при загрузке крупных файлов. Сравнить с подходом Cursor (context window включает открытые файлы) и Augment (lazy file resolution).
- **Реализация**: Добавить механизм `LoadFileIntoBlackboard()`, позволяющий агенту загружать содержимое файлов в Blackboard для кросс-шагового доступа. Поддержка ленивой загрузки (содержимое считывается при обращении, а не при добавлении). Инъекция загруженных файлов в контекст executor'а при выполнении шагов, с дедупликацией относительно файлов, уже присутствующих в контексте из tool-вызовов.

---

## Трек 2: Мультиагентная оркестрация

**Текущее состояние**: DAG-based параллельное выполнение уже реализовано (`FindReadySteps()` + `RunSubAgentsParallel()` с горутинами на каждый независимый шаг). Профили агентов-специалистов определены (`AgentProfile` с ролями researcher/coder/tester/executor, role-specific system prompt suffixes и tool pruning defaults) и планировщик инструктирован назначать их. Однако нет агента-верификатора (Reflector анализирует сбои, но не проверяет соответствие требованиям), нет дебатов/консенсуса.

### 2.1 Улучшение параллельного выполнения: обнаружение конфликтов и изоляция

> **Статус**: частично реализовано -- параллельный запуск горутин и трекинг файловых изменений по шагам (`SetStepFileChanges()`) работают. Отсутствует: обнаружение конфликтов при записи в один файл из двух шагов, стратегия разрешения конфликтов.

- **Исследование**: Изучить модель оптимистичной конкурентности Cursor (параллельные чтения, отклонение устаревших записей). Оценить git worktree vs. изоляция в памяти. Профилировать реальную частоту конфликтов в типичных DAG-планах.
- **Реализация**: Добавить обнаружение конфликтов на уровне файлов при завершении шага (два шага модифицируют один файл -> промпт на разрешение конфликта или автоматический merge). Рассмотреть git-worktree изоляцию для шагов высокой сложности.

### 2.2 Углубление профилей агентов-специалистов

> **Статус**: частично реализовано -- `AgentProfile` тип определён в `core/types.go`, role suffixes и tool pruning defaults настроены в `core/stepconfig.go`, планировщик инструктирован в `core/planner.go` (строка 73: «Assign specialized profiles when it adds clear value»). Однако: профили опциональны и не всегда назначаются; Router не выводит рекомендацию профиля; сложность в `applyCompactionStrategy` захардкожена на 3.

- **Исследование**: Собрать метрики реальных сессий: как часто планировщик назначает профили? Улучшается ли результат с профилями vs. без? Какие роли нужны дополнительно (reviewer, documenter)?
- **Реализация**: Расширить Router для рекомендации профилей на основе домена и сложности. Добавить профили `reviewer` и `documenter`. Передавать реальную сложность (от Router) в `applyCompactionStrategy()` вместо захардкоженной 3. Сделать назначение профилей обязательным для сложности >= 3.

### 2.3 Агент-верификатор (post-execution verification)

> **Статус**: частично реализовано -- `ToolJudge` существует для безопасности (auto-approve vs. confirm мутирующих операций), `Reflector` анализирует сбои выполнения (root cause, suggested action). Однако: ни один из них не проверяет соответствие результатов исходным требованиям, не запускает компиляцию/тесты, не оценивает полноту.

- **Исследование**: Изучить отдельную модель-судью Cursor и «verification lattice» от Anthropic (детерминистический -> семантический -> безопасность -> агентный -> человеческий).
- **Реализация**: После каждого выполнения DAG запускать агент `verifier`, который ревьюит изменения относительно исходного запроса. Проверки: код компилируется, тесты проходят, требования выполнены, нет регрессий. Отчёт передаётся как рефлексия для повтора при обнаружении проблем.

### 2.4 Режим дебатов для сложных решений

> **Статус**: отсутствует -- Router возвращает одно решение, Planner генерирует один план, Orchestrator выполняет один план.

- **Исследование**: Оценить эффективность мульти-предложений с выбором (подход Augment «exploit non-determinism»). Бенчмарк: превосходит ли запуск 3 параллельных реализаций с выбором однопроходное выполнение?
- **Реализация**: Для задач сложности 4-5 планировщик генерирует 2-3 альтернативных подхода. Каждый выполняется изолированно (git worktree или в памяти). Агент-верификатор выбирает лучший результат. Стоимость: 3x вычислений только для задач максимальной сложности.

### 2.5 Режим исследования с гипотезами

> **Статус**: отсутствует -- агент выполняет задачи последовательно (route -> plan -> execute), нет режима итеративного исследования с формированием и проверкой гипотез.

- **Исследование**: Изучить паттерны research-агентов (итеративный поиск -> гипотеза -> верификация -> уточнение). Оценить интеграцию с текущим циклом Router -> Planner -> Executor. Сопоставить с подходом ReAct и Tree-of-Thought. Проанализировать, как гипотезы представлены в существующих фреймворках (LangGraph, AutoGen) и какие типы верификации применимы (детерминистические: тесты/линт, семантические: LLM-judge).
- **Реализация**: Добавить режим `research`, в котором агент формулирует гипотезы, проверяет их через инструменты (поиск, чтение файлов, запуск кода), уточняет или отбрасывает на основе результатов. Хранить гипотезы в Blackboard со статусами (proposed/confirmed/refuted). Интегрировать с Треком 1 (персистентные гипотезы как тип фактов) и Треком 4 (адаптивная стратегия компактизации для исследований с большим контекстом).

---

## Трек 3: Конвейер автоматической верификации

**Текущее состояние**: Агент может запускать тесты/линтер через `bash_exec`, но только если явно проинструктирован (в промпте или пользователем). Нет автоматического запуска верификации после изменений кода. Нет автоопределения build/test/lint команд проекта. Нет генерации тестов. Нет обнаружения регрессий.

### 3.1 Детерминистические гейты верификации

> **Статус**: отсутствует -- нет `ProjectDetector`, нет автоопределения команд, нет автоматического post-modification конвейера.

- **Исследование**: Проанализировать типичные структуры проектов для автоопределения команд сборки/линтинга/тестирования (Makefile, package.json, Cargo.toml, pyproject.toml). Сопоставить со стандартными гейтами.
- **Реализация**: Добавить `ProjectDetector` в `backend/`, обнаруживающий доступные команды верификации при открытии проекта. Сохранять в конфигурации проекта. После каждой модификации кода агентом автоматически запускать конвейер: `lint -> typecheck -> test`. Если любой гейт падает, инжектировать результат в контекст агента как tool_result и дать ему возможность самокоррекции.

### 3.2 Целевая генерация тестов

> **Статус**: отсутствует.

- **Исследование**: Изучить R-MCTS подходы для генерации тестов (статья ExACT). Оценить извлечение сигнатур функций через tree-sitter для скаффолдинга тестов.
- **Реализация**: Добавить инструмент `generate_tests`, который: (1) извлекает сигнатуры функций через tree-sitter, (2) генерирует тест-кейсы с помощью LLM с контекстом функции + существующими паттернами тестов, (3) запускает тесты, (4) итерирует при неудачах. Фокус первоначально на Go (table-driven) и TypeScript (vitest/jest).

### 3.3 Обнаружение регрессий

> **Статус**: отсутствует.

- **Исследование**: Оценить стратегии обнаружения регрессий: snapshot-тестирование, сравнение golden-файлов, регрессия бенчмарков (пороги p95 латентности).
- **Реализация**: До и после изменений агента фиксировать: результаты тестов, показатели бенчмарков, размер бинарника, покрытие типами. Вычислять diff и сообщать о регрессиях. Блокировать `finish`, если обнаружена регрессия без явного override от пользователя.

### 3.4 Интеграция сканирования безопасности

> **Статус**: отсутствует.

- **Исследование**: Оценить встраиваемые SAST-инструменты (правила semgrep, gosec для Go, eslint-plugin-security для TS). Сравнить подход MCP-сервера vs. встроенный инструмент.
- **Реализация**: Добавить инструмент `security_scan` (запускает сконфигурированный сканер на рабочем пространстве). Интегрировать в конвейер верификации. Агент получает результаты сканирования и обязан устранить находки high/critical до завершения.

---

## Трек 4: Развитие контекстной инженерии

**Текущее состояние**: Реализовано больше, чем предполагалось изначально:

- **Tool output pruning** (`toolOutputPruning`): конфигурируемый механизм очистки старых tool-результатов с заменой на placeholder-текст, защитой определённых инструментов (`store_fact`, `search_facts`), порогом активации (50% заполнения контекста), настройкой `keepLastN: 3`.
- **Адаптивный выбор стратегии**: `applyCompactionStrategy()` в `core/router.go` выбирает стратегию по домену и сложности (code -> sliding_window, research -> summarization, mixed с complexity >= 4 -> hierarchical). Однако complexity захардкожена на 3 при применении к шагам.
- **Auto-RAG**: `injectVectorSearchHints()` в `core/orchestrator.go` автоматически запускает семантический поиск перед маршрутизацией/планированием, инжектируя top-5 релевантных файлов как секцию «Relevant Project Files» в системный промпт.
- **Prompt caching**: `CacheBreakMarker` инфраструктура в `sdk/prompt/builder.go`, Anthropic provider применяет `CacheControl{Type: Ephemeral}` ко всем стабильным частям системного промпта.

### 4.1 Эволюция tool output pruning до полноценной стратегии clearing

> **Статус**: частично реализовано -- `toolOutputPruning` работает (placeholder текст, protected tools, keepLastN, threshold). Отличие от целевого состояния: текущая реализация работает как прунинг старых результатов, а не как хирургическая замена с сохранением `tool_use` записей для переиспользования.

- **Исследование**: Измерить эффективность текущего pruning: сколько токенов экономится, как часто агент повторно запрашивает очищенные данные? Сравнить текущий подход (полное удаление tool_result) с подходом Anthropic (сохранение tool_use + placeholder в tool_result).
- **Реализация**: Рефакторить pruning для сохранения `tool_use` записей (агент видит, что он запрашивал, но не видит результат). Добавить интеллектуальный placeholder с указанием типа очищенных данных («файл X был прочитан, результат очищен -- перечитайте при необходимости»). Добавить метрики: процент повторных запросов после очистки.

### 4.2 Доработка адаптивного выбора стратегии

> **Статус**: частично реализовано -- `applyCompactionStrategy()` выбирает по домену, но complexity захардкожена на 3 при конфигурации шагов. Нет динамической адаптации к runtime-характеристикам задачи.

- **Исследование**: Профилировать, какая стратегия компактизации лучше работает для каких типов задач. Собрать метрики из реальных сессий: соотношение tool-вызовов, средний размер tool-результатов, частота переиспользования ранних результатов.
- **Реализация**: Передавать реальную сложность от Router в `coreStepConfigurator` вместо захардкоженной 3. Добавить `CompactionAnalyzer`, который может переключать стратегию на лету при обнаружении паттернов (например, много мелких tool-вызовов -> tool_result_clearing, мало больших -> hierarchical).

### 4.3 Углубление RAG-инъекции

> **Статус**: частично реализовано -- `injectVectorSearchHints()` инжектирует top-5 файловых путей с аннотацией перед маршрутизацией. Планировщик имеет доступ к `semantic_search` как инструменту Tier 1. Однако: инжектируются только пути файлов, а не фрагменты кода; инжекция происходит только перед routing, не перед каждым ходом executor'а; нет дедупликации с уже прочитанными файлами.

- **Исследование**: Бенчмарк влияния на quality: инъекция путей vs. инъекция фрагментов кода. Оценить overhead дополнительного семантического поиска перед каждым ходом executor'а.
- **Реализация**: Расширить `injectVectorSearchHints()` для инъекции фрагментов кода (первые N строк релевантных чанков), а не только путей. Добавить опциональную инъекцию перед ходами executor'а для задач высокой сложности. Дедуплицировать относительно файлов, уже присутствующих в контексте из tool-вызовов.

### 4.4 Расширение prompt caching на другие провайдеры

> **Статус**: частично реализовано -- CacheBreakMarker инфраструктура работает, Anthropic provider применяет ephemeral cache control к стабильным частям. Не реализовано для Gemini, OpenAI.

- **Исследование**: Профилировать процент попаданий в кэш для Anthropic. Изучить механизмы кэширования Gemini (implicit caching) и OpenAI (server-side). Измерить экономию стоимости.
- **Реализация**: Адаптировать CacheBreak для Gemini provider (context caching API). Для OpenAI-compatible провайдеров оценить, даёт ли стабильность порядка системных секций неявное кэширование. Добавить метрику `cache_hit_rate` в сессионную статистику.

---

## Трек 5: Мультимодальность и управление компьютером

**Текущее состояние**: Полностью отсутствует. `Message` struct содержит только `Content string`, без поддержки multipart. Все провайдеры (Anthropic, Gemini, OpenAI) создают только текстовые блоки. Фронтенд ChatInput не поддерживает drag-and-drop/paste изображений. Флаг `ModelCapabilities.Attachment` определён, но не используется.

### 5.1 Ввод изображений/скриншотов

> **Статус**: отсутствует.

- **Исследование**: Проанализировать, какие текущие LLM-провайдеры поддерживают vision (Anthropic, Gemini, GPT-4+). Сопоставить с флагами capability, уже существующими в `ModelCapabilities.Attachment`.
- **Реализация**: Расширить `sdk/llm/message.go`: добавить `ContentParts []ContentPart` наряду с `Content string`. Каждый `ContentPart` поддерживает тип `text` или `image` (base64 + media type). Обновить все провайдеры для конвертации в нативный формат (Anthropic: image content block, Gemini: InlineData part, OpenAI: image_url). Фронтенд: drag-and-drop / вставка изображения в ChatInput. Кейс: «исправь баг, показанный на этом скриншоте».

### 5.2 Инструмент захвата скриншотов

> **Статус**: отсутствует.

- **Исследование**: Оценить кроссплатформенные библиотеки для скриншотов в Go (macOS: CLI screencapture, Linux: import/scrot). Рассмотреть возможности Wails WebView2.
- **Реализация**: Добавить встроенный инструмент `capture_screenshot`, делающий скриншот основного дисплея или конкретного окна. Возвращает base64-изображение для анализа LLM. Кейс: агент визуально проверяет изменения UI.

### 5.3 Инструмент автоматизации браузера

> **Статус**: отсутствует -- нет chromedp, playwright-go или аналогов в go.mod.

- **Исследование**: Оценить варианты: chromedp (Go, Chrome DevTools Protocol), playwright-go, или MCP-подход (browser-use MCP server).
- **Реализация**: Начать с MCP-подхода (рекомендовать browser-use MCP-сервер в конфигурации по умолчанию). Позднее добавить нативный инструмент `browser_action` с операциями: navigate, click, type, screenshot, extract_text.

### 5.4 Конвейер «Диаграмма -> Код»

> **Статус**: отсутствует (зависит от 5.1).

- **Исследование**: Оценить эффективность генерации кода из Mermaid/PlantUML -> архитектура. Изучить текущие возможности мультимодальных моделей по интерпретации рисованных от руки диаграмм.
- **Реализация**: Добавить инструмент `interpret_diagram`, принимающий изображение, использующий vision-модель для извлечения структуры, выводящий Mermaid/JSON представление. Планировщик может использовать это для декомпозиции реализации из архитектурных диаграмм.

---

## Трек 6: Headless / CLI / Серверный режим

**Текущее состояние**: Только десктопный режим через Wails. Единственная точка входа -- `main.go` -> `desktop.NewApp()` -> `wails.Run()`. Директория `cmd/` не существует. Однако `core/` и `sdk/` не зависят от Wails, что делает выделение CLI/server реалистичным.

### 6.1 Headless-запуск агента

> **Статус**: отсутствует (но архитектурный фундамент готов -- core/sdk Wails-независимы).

- **Исследование**: Спроектировать границу между Wails-зависимым кодом (`desktop/`) и чисто Go-исполнением агента. Определить минимальный набор адаптеров для headless режима (event emitter -> stdout, confirmation -> stdin).
- **Реализация**: Создать точку входа `cmd/c0wrk-cli/`, инстанциирующую `core.Orchestrator` напрямую, читающую конфигурацию, принимающую задачу из stdin/аргументов, стримящую вывод в stdout. Без UI, без зависимости от Wails. Открывает: CI-интеграцию, скриптинг, shell-пайплайны.

### 6.2 HTTP-серверный режим

> **Статус**: отсутствует.

- **Исследование**: Оценить дизайн API: REST vs. gRPC vs. WebSocket для стриминга. Изучить асинхронный API-паттерн OpenAI Codex (отправить задачу -> опрашивать статус -> получить результат).
- **Реализация**: Создать `cmd/c0wrk-server/` с HTTP API: `POST /tasks` (отправка), `GET /tasks/{id}` (статус), `GET /tasks/{id}/stream` (SSE). Обеспечить удалённое выполнение агента для командных сценариев. Аутентификация через API-ключи.

### 6.3 Интеграция с CI/CD-конвейерами

> **Статус**: отсутствует.

- **Исследование**: Сопоставить популярные CI-системы (GitHub Actions, GitLab CI). Спроектировать «ci-режим», где агент получает контекст issue/PR и производит изменения.
- **Реализация**: Добавить `c0wrk-ci` action/команду, которая: (1) клонирует репозиторий, (2) читает описание issue/PR, (3) выполняет задачу, (4) запускает конвейер верификации, (5) создаёт PR/пушит фиксы. Первый таргет -- GitHub Actions.

### 6.4 Поддержка протокола A2A

> **Статус**: отсутствует.

- **Исследование**: Изучить спецификацию протокола Agent-to-Agent от Google. Оценить: лучше ли c0wrk в роли A2A-сервера (принимает задачи) или клиента (делегирует другим агентам)?
- **Реализация**: Реализовать серверный интерфейс A2A, чтобы внешние оркестраторы могли делегировать задачи по кодированию в c0wrk. Реализовать клиентский A2A, чтобы c0wrk мог делегировать подзадачи (например, «запустить security-сканирование» специализированному агенту).

### 6.5 Поддержка протокола ACP

> **Статус**: отсутствует -- нет реализации Agent Communication Protocol, нет клиентских/серверных адаптеров.

- **Исследование**: Изучить спецификацию ACP (Agent Communication Protocol). Сравнить с A2A (6.4) и определить область пересечения и различия. Оценить: какой протокол даёт лучшую совместимость с существующими агентными фреймворками. Проанализировать требования к сериализации артефактов (сообщения, файлы, инструменты) в формате ACP.
- **Реализация**: Реализовать ACP-совместимый интерфейс для c0wrk. В серверном режиме (6.2) поддерживать приём задач по ACP наряду с HTTP API. В клиентском режиме -- делегирование подзадач ACP-совместимым агентам. Обеспечить совместимость с A2A при необходимости через адаптер.

---

## Трек 7: Улучшение Developer Experience и эволюция UI

**Текущее состояние**: UI настроек покрывает LLM, MCP, Security, Search, Log Level, но не покрывает executor tuning (maxReactSteps, maxRetries), compaction thresholds, tool limits, timeouts. Трекинг токенов реализован (StatusBar показывает input/output tokens), но нет расчёта стоимости. Blackboard отслеживает файловые изменения (`SetStepFileChanges`), но нет UI для пофайлового ревью/одобрения. Нет автоопределения build-системы проекта.

### 7.1 Интерактивный ревью изменений

> **Статус**: частично реализовано -- инфраструктура (Blackboard tracking файловых изменений, FileViewer с diff-отображением) есть. Отсутствует: UI workflow accept/reject для предложенных изменений.

- **Исследование**: Изучить diff-view Cursor и опыт ревью PR в Copilot. Спроектировать flow «pending changes», где агент предлагает изменения, а пользователь подтверждает/отклоняет пофайлово.
- **Реализация**: Добавить панель `PendingChanges`, показывающую все файлы, модифицированные агентом в текущей задаче (данные из `GetSessionFileChanges()`). Для каждого файла: unified diff с кнопками accept/reject. Массовое подтверждение/отклонение. Изменения применяются только после одобрения пользователем.

### 7.2 Расширенный UI настроек

> **Статус**: отсутствует -- executor, compaction, tool limits и timeout параметры доступны только через config.yaml.

- **Реализация**: Вынести оставшиеся параметры config.yaml в UI: лимиты цикла исполнителя (maxReactSteps, maxRetries), пороги компактизации (predictive/warning/emergency %), лимиты инструментов (строки чтения, результаты ripgrep), таймауты (bash, web). Группировать как вкладку «Advanced» в SettingsModal.

### 7.3 Дашборд стоимости и бюджета

> **Статус**: частично реализовано -- токены отслеживаются и отображаются в StatusBar (`formatTokenCount(tokens.total_input_tokens)↑ / ↓`). Модель стоимости определена в `ModelMetadata` (InputCostPer1M, OutputCostPer1M). Не реализовано: расчёт стоимости, бюджетные лимиты, дашборд.

- **Исследование**: Профилировать реальные паттерны использования: токены на задачу, стоимость сессии, стоимость проекта. Спроектировать бюджетные контроли.
- **Реализация**: Добавить компонент `CostDashboard`, использующий существующую метаданные `InputCostPer1M`/`OutputCostPer1M` для расчёта стоимости в реальном времени. Показывать: текущая стоимость сессии, кумулятивная стоимость проекта. Добавить бюджетные лимиты: максимальная стоимость за задачу, за день. Агент приостанавливается и спрашивает пользователя при приближении к лимиту.

### 7.4 Визард онбординга проекта

> **Статус**: отсутствует.

- **Исследование**: Изучить концепцию «agent-ready repo». Что делает репозиторий удобным для агентов? (AGENTS.md, команды тестирования, команды сборки, архитектурная документация).
- **Реализация**: При первом открытии проекта запускать автоопределение: система сборки (Make/npm/cargo), тестовый фреймворк, линтер, языковой состав, ключевые директории. Генерировать предложенный AGENTS.md. Создать профиль проекта в БД с обнаруженными командами.

### 7.5 Режим коллаборативного редактирования

> **Статус**: отсутствует -- CodeMirror используется в read-only режиме.

- **Исследование**: Оценить возможность инлайн-редактирования кода в панели чата (мини-редактор CodeMirror). Изучить UX принятия инлайн-diff в Cursor.
- **Реализация**: Для небольших изменений кода рендерить редактируемый инлайн-diff в чате. Пользователь может модифицировать предложение агента перед применением. Уменьшает переключение контекста между чатом и файловым просмотрщиком.

### 7.6 Специализированные события для известных инструментов

> **Статус**: отсутствует -- все tool-вызовы отображаются как универсальные события одного типа (`tool` в `DisplayItem[]`), без специализированного рендеринга для конкретных инструментов.

- **Исследование**: Проанализировать Top-15 инструментов по частоте использования в реальных сессиях. Спроектировать специализированные card-компоненты для каждого: `bash_exec` (терминальный вывод с ANSI-подсветкой), `write_file`/`edit_file` (inline diff-превью), `search` (кликабельный список результатов), `web_fetch` (превью страницы). Оценить влияние на производительность рендеринга при большом количестве tool-событий.
- **Реализация**: Добавить специализированные компоненты рендеринга для известных инструментов в чате. Каждый инструмент получает свой визуальный формат: `bash_exec` -- collapsible терминал, `write_file`/`edit_file` -- inline diff с навигацией в File Viewer, `search` -- кликабельный список файлов. Улучшает читаемость чата и снижает когнитивную нагрузку при ревью действий агента.

### 7.7 Навигация по ссылкам на локальные файлы в Markdown

> **Статус**: отсутствует -- Markdown-рендеринг в чате (react-markdown + rehype/remark-плагины) не обрабатывает ссылки на локальные файлы (вида `[file](./path/to/file.go)`), такие ссылки не кликабельны и не открывают File Viewer.

- **Исследование**: Изучить существующие rehype/remark-плагины для обработки локальных ссылок. Определить формат ссылок, поддерживаемый агентом (относительные пути от корня проекта, абсолютные пути). Оценить безопасность: предотвратить навигацию за пределы рабочего пространства.
- **Реализация**: Расширить Markdown-рендер для распознавания локальных файловых ссылок. При клике -- открывать файл в File Viewer с подсветкой синтаксиса. Поддержка навигации к конкретной строке (формат: `file.go#L42`). Обучить агента (через системный промпт) использовать локальные ссылки при упоминании файлов в ответах.

### 7.8 Редактор Blackboard

> **Статус**: отсутствует -- Blackboard доступен агенту через API (`StoreFact`/`GetFacts`/`SetStepFileChanges`), но нет UI для просмотра и редактирования содержимого пользователем. Фронтенд-стор `blackboardStore` определён, но не используется в компонентах.

- **Исследование**: Спроектировать UI Blackboard: какие данные показывать (факты, файловые изменения, метаинформация по шагам), как обеспечить интуитивное редактирование фактов, как визуализировать связи между шагами и фактами. Оценить расположение: вкладка в sidebar или отдельная панель.
- **Реализация**: Добавить панель Blackboard Viewer в интерфейс (доступна из sidebar). Показывать: факты с возможностью редактирования/удаления, файловые изменения по шагам, метаинформацию выполнения. Пользователь может добавлять факты вручную, удалять устаревшие, корректировать информацию. Интегрировать с `blackboardStore`.

---

## Трек 8: Эволюция эмбеддингов и кодового поиска

**Текущее состояние**: Единственная модель эмбеддингов Jina v2-small (512-dim, 512-token max), захардкоженная в `sdk/embedding/embedder.go`, не конфигурируемая через config.yaml. Инкрементальная индексация реализована на уровне файлов (`IndexIncremental()` обнаруживает stale/new/deleted файлы через хеши), но при изменении файла все его чанки переэмбеддиваются. Нет FTS5/BM25 для гибридного поиска. Нет кросс-проектного поиска.

### 8.1 Гибридный поиск (Вектор + Ключевые слова + Структура)

> **Статус**: отсутствует -- `semantic_search` использует только вектор, нет FTS5/BM25 индекса.

- **Исследование**: Бенчмарк: только-вектор vs. только-ключевые-слова vs. гибрид на задачах кодового поиска. Оценить интеграцию BM25 с существующим векторным индексом.
- **Реализация**: Добавить FTS5-индекс параллельно с векторным. Инструмент `semantic_search` запускает оба, объединяет результаты через reciprocal rank fusion. AST-aware буст: если запрос упоминает имя функции, повышать вес точных совпадений.

### 8.2 Обновление и конфигурируемость модели эмбеддингов

> **Статус**: отсутствует -- модель захардкожена (Jina v2-small, 512-dim), нет секции конфигурации в config.yaml.

- **Исследование**: Бенчмарк более новых code-моделей эмбеддингов (варианты CodeBERT, Nomic Embed Code, Voyage Code v2). Оценить совместимость с ONNX и скорость инференса.
- **Реализация**: Добавить секцию `embedding:` в config.yaml (model_path, dimensions, max_seq_length). Поддержка нескольких моделей. Добавить инструмент миграции для переиндексации с новой моделью. Цель: более высокая размерность (768-1024), больший контекст (2048 токенов), лучшее понимание кода.

### 8.3 Улучшение гранулярности инкрементальной индексации

> **Статус**: частично реализовано -- `IndexIncremental()` определяет stale/new/deleted файлы по хешам, переиндексирует только изменённые. Однако: при изменении файла все его чанки переэмбеддиваются (нет chunk-level diff).

- **Исследование**: Профилировать: какая доля чанков в файле реально меняется при типичном редактировании? Оценить: стоит ли chunk-level diff по сравнению с file-level re-embedding?
- **Реализация**: При изменении файла вычислять хеши отдельных чанков, переэмбеддировать только изменённые. Для большинства файлов (< 10 чанков) экономия может быть незначительной -- фокусировать на крупных файлах.

### 8.4 Кросс-проектный поиск

> **Статус**: отсутствует.

- **Исследование**: Оценить кейсы поиска по нескольким проектам (общие библиотеки, компоненты монорепо).
- **Реализация**: Добавить инструмент `search_across_projects`, запрашивающий векторные индексы нескольких проектов. Полезен для: поиска, как аналогичные задачи были решены в другом месте, обнаружения общих утилит.

---

## Трек 9: Specification-Driven Development (разработка через спецификации)

**Текущее состояние**: Спецификации существуют как документы разработчика (`specs/`, `.qoder/specs/`), но не как runtime-артефакты агента. Планировщик генерирует `description` с секцией «Acceptance Criteria» в шагах плана. Reflector анализирует выполнение критериев приёмки (упоминает «3 out of 5 acceptance criteria passed» в промпте). Однако: нет инструмента создания спецификаций, нет трассируемости spec -> code, планировщик не принимает спецификации как входные данные.

### 9.1 Инструмент создания спецификаций

> **Статус**: отсутствует.

- **Исследование**: Изучить статью SDD (arxiv:2602.00180) и spec-first workflow Augment Intent. Спроектировать формат спецификации (структурированный markdown с требованиями, критериями приёмки, ограничениями).
- **Реализация**: Добавить инструмент/режим `spec_author`, где пользователь описывает намерение, а агент производит структурированную спецификацию. Спецификация хранится как markdown в проекте. Планировщик использует спецификацию как вход (не только сообщение пользователя).

### 9.2 Конвейер «Спецификация -> План -> Код»

> **Статус**: отсутствует -- планировщик принимает текстовое описание задачи, не структурированную спецификацию.

- **Исследование**: Оценить: может ли документ спецификации полностью заменить фазу router+planner? Или дополнить?
- **Реализация**: При наличии спецификации планировщик генерирует DAG непосредственно из секций спецификации. Каждое требование спецификации сопоставляется с одним или более шагами DAG. Трассируемость: вывод каждого шага ссылается на то, какое требование спецификации он выполняет.

### 9.3 Приёмочное тестирование по спецификации

> **Статус**: частично реализовано -- Reflector умеет анализировать acceptance criteria (упоминается в промпте), но критерии берутся из описания шагов плана, а не из внешней спецификации.

- **Исследование**: Изучить, как критерии приёмки могут быть автоматически верифицированы (некоторые тестируемы, некоторые требуют суждения LLM).
- **Реализация**: После выполнения агент-верификатор проверяет каждое требование спецификации относительно фактических изменений. Детерминистические проверки (тесты проходят, линт чист) + семантические проверки (LLM ревьюит, выполнено ли требование). Отчёт: требование-по-требованию pass/fail.

---

## Трек 10: Усиление безопасности и governance

**Текущее состояние**: Модель безопасности включает: 3-уровневые политики инструментов (`always_allow` / `always_deny` / `user_confirm`), LLM-based `ToolJudge` для автоматической оценки безопасности мутирующих операций с кэшем вердиктов, regex-блоклист для bash_exec. Однако: нет песочницы (bash_exec выполняет `exec.CommandContext` напрямую), нет персистентного аудит-трейла, нет policy DSL, нет сканирования секретов в write/edit.

### 10.1 Песочница выполнения

> **Статус**: отсутствует -- только regex blacklist + timeout, без контейнеризации или process isolation.

- **Исследование**: Оценить варианты песочницы: Docker-контейнеры, macOS sandbox-exec, nsjail. Профилировать влияние на производительность vs. прирост безопасности.
- **Реализация**: Добавить опциональный sandbox-режим для bash_exec: команды выполняются в изолированном контейнере/песочнице с примонтированным рабочим пространством (read-only или ограниченная запись). Исходящий сетевой трафик заблокирован по умолчанию. Включение через секцию security в config.yaml.

### 10.2 Полноценный аудит-трейл

> **Статус**: отсутствует -- логирование через slog (in-process), без персистентного аудит-лога.

- **Исследование**: Спроектировать схему аудита: какие события логировать (все tool-вызовы, все LLM-запросы, все решения пользователя), политика хранения, формат экспорта.
- **Реализация**: Добавить SQLite-таблицу `audit_log`, фиксирующую каждый tool-вызов (tool, args, result_hash, policy_decision, user_verdict, timestamp). Добавить API `export_audit` для compliance-ревью. UI: просмотрщик аудит-лога в настройках.

### 10.3 Policy-as-Code

> **Статус**: отсутствует -- текущая политика: enum (3 уровня) + regex blacklist. Нет OPA, Cedar или аналогов.

- **Исследование**: Изучить OPA (Open Policy Agent) и Cedar для оценки политик. Оценить: встроить движок политик или использовать простой rule engine.
- **Реализация**: Заменить текущий regex-based блоклист на движок policy-as-code. Политики определяют: какие инструменты могут обращаться к каким путям, какие сетевые назначения разрешены, какие модификации файлов допустимы. Политики коммитятся в репозиторий как `.c0wrk/policies/`.

### 10.4 Обнаружение секретов

> **Статус**: отсутствует -- write_file и edit_file не сканируют содержимое на секреты.

- **Исследование**: Оценить встраиваемые сканеры секретов (правила gitleaks, паттерны trufflehog). Изучить: должен ли агент отказываться коммитить файлы с секретами?
- **Реализация**: Добавить pre-write сканирование секретов: перед `write_file` или `edit_file` сканировать контент на API-ключи, токены, пароли. При обнаружении -- предупредить пользователя и потребовать явный override. Интегрировать с аудит-трейлом.

### 10.5 Гардрейлы на базе детекторов dunetrace

> **Статус**: отсутствует -- текущая безопасность основана на статических политиках (enum + regex blacklist + LLM-судья), нет динамических детекторов аномалий в поведении агента.

- **Исследование**: Изучить фреймворк [dunetrace](https://github.com/dunetrace/dunetrace) и его детекторы (prompt injection, tool misuse, data exfiltration, jailbreak и др.). Оценить совместимость с текущим конвейером выполнения агента. Профилировать overhead добавления runtime-детекции к каждому tool-вызову. Сравнить с существующим `ToolJudge`: пересечение и дополнение.
- **Реализация**: Интегрировать детекторы dunetrace как гардрейлы в цикл выполнения агента. Каждый tool-вызов проходит через детекторы перед выполнением. При срабатывании -- блокировка вызова, логирование инцидента, уведомление пользователя. Конфигурируемые пороги чувствительности через секцию `security:` в config.yaml. Интеграция с аудит-трейлом (10.2).

---

## Матрица приоритетов

| Приоритет                          | Трек                                     | Обоснование                                                                                                                |
| ---------------------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **P0** (ключевой дифференциатор)   | T1.1 Расширение фактов до уровня проекта | Персистентность фактов уже есть (task-level), но кросс-сессионный project-wide поиск -- критический разрыв с конкурентами. |
| **P0**                             | T1.2 Проектная база знаний               | У всех конкурентов (Claude Code, Cursor, Aider) есть кросс-сессионная память.                                              |
| **P0**                             | T3.1 Гейты верификации                   | Автоопределение build/test/lint и системная верификация после изменений -- стандарт для агентов 2026 года.                 |
| **P1** (конкурентное преимущество) | T2.1 Конфликты параллельного выполнения  | Параллельные горутины есть, но без обнаружения конфликтов при записи в один файл.                                          |
| **P1**                             | T2.2 Углубление профилей                 | Инфраструктура есть, но профили редко назначаются; complexity захардкожена.                                                |
| **P1**                             | T5.1 Ввод изображений                    | Vision-модели стандартны. `ModelCapabilities.Attachment` уже определён.                                                    |
| **P1**                             | T6.1 CLI-режим                           | core/sdk уже Wails-независимы -- effort минимален.                                                                         |
| **P1**                             | T7.1 UX ревью изменений                  | Blackboard tracking файлов уже есть -- нужен только UI workflow.                                                           |
| **P1**                             | T7.3 Дашборд стоимости                   | Token tracking + ModelMetadata с ценами уже есть -- нужен только расчёт и UI.                                              |
| **P1**                             | T7.6 Специализированные события          | Существенный прирост UX-читаемости чата; Top-15 инструментов покрывают 90% событий.                                        |
| **P1**                             | T1.5 Подгрузка файлов в Blackboard       | Кросс-шаговый доступ к файлам -- ключ к качественной многошаговой работе.                                                  |
| **P2** (стратегический)            | T4.1-4.2 Эволюция pruning и адаптивности | Tool output pruning работает, но можно улучшить; complexity захардкожена.                                                  |
| **P2**                             | T1.3 Граф знаний                         | Глубокое понимание кодовой базы через графовые запросы.                                                                    |
| **P2**                             | T8.1 Гибридный поиск                     | Лучшее качество поиска транслируется в лучшую производительность агента.                                                   |
| **P2**                             | T9.1-9.2 Разработка через спецификации   | Зарождающаяся парадигма. Преимущество первопроходца.                                                                       |
| **P2**                             | T2.3 Агент-верификатор                   | ToolJudge + Reflector уже есть, но не покрывают полную верификацию.                                                        |
| **P2**                             | T2.5 Режим исследования с гипотезами     | Итеративное исследование -- качественно новый паттерн работы агента.                                                       |
| **P2**                             | T7.7 Навигация по локальным файлам       | Низкий effort, высокий impact на навигацию по результатам агента.                                                          |
| **P2**                             | T7.8 Редактор Blackboard                 | Дополняет T1.1 -- пользовательский контроль над памятью агента.                                                            |
| **P2**                             | T10.5 Гардрейлы dunetrace                | Динамическая детекция аномалий дополняет статические политики.                                                             |
| **P3** (перспективный)             | T5.3 Автоматизация браузера              | MCP-подход доступен сейчас.                                                                                                |
| **P3**                             | T6.2-6.4 Сервер/CI/A2A                   | Платформенная игра.                                                                                                        |
| **P3**                             | T6.5 Протокол ACP                        | Спецификация ещё формируется; риск раннего внедрения.                                                                      |
| **P3**                             | T10.1-10.4 Усиление безопасности         | Важно для enterprise-адопции.                                                                                              |

---

## Исследовательские задачи (без кода, чистая разведка)

Это исследования, которые должны обосновать будущие решения:

1. **Shootout моделей эмбеддингов**: Бенчмарк Jina v2-small vs. Nomic Embed Code vs. Voyage Code v2 на реальных запросах кодового поиска c0wrk. Метрики: recall@10, латентность, размер модели. Решить, стоит ли обновление.

2. **Исследование эффективности стратегий компактизации**: Инструментировать существующие сессии для сбора: какая стратегия компактизации сработала, сколько было потеряно, нуждался ли агент в повторно полученных данных? Квантифицировать потерю информации по стратегиям. Включить анализ текущего tool output pruning.

3. **Анализ показателя успешности агента**: Определить метрику «успех задачи». Инструментировать: задачи попытаны, задачи выполнены без повтора, задачи выполнены с повтором, задачи заброшены. Коррелировать с: сложность задачи, модель, бюджет токенов, назначение профиля агента.

4. **Оценка на SWE-bench**: Запустить c0wrk на подмножестве SWE-bench Lite. Установить базовую линию. Выявить систематические режимы отказа (неправильный файл найден, некорректный паттерн редактирования, тест не запущен и т.д.). Использовать как regression-suite для улучшений.

5. **Исследование пригодности локальных моделей**: Бенчмарк Qwen3, DeepSeek V3 и LLaMA 4 через LM Studio на типичных задачах c0wrk. Метрики: процент завершения задач, эффективность по токенам, латентность. Определить: могут ли локальные модели заменить облачные для рутинных задач?

6. **Оценка интеграции Tree-Sitter**: Оценить парсеры tree-sitter для Go/TS/Python в Go. Измерить: скорость парсинга для типичных репозиториев, качество AST, сложность интеграции. Решить: встроить в sdk или использовать как MCP-сервер.

7. **Аудит эффективности профилей агентов**: Собрать данные из реальных сессий -- как часто планировщик назначает профили? Коррелирует ли назначение профиля с успехом шага? Какие профили наиболее/наименее полезны?

8. **Профилирование cache hit rate**: Для Anthropic provider измерить процент попаданий в prompt cache после внедрения CacheBreakMarker. Оценить экономию в $ на типичную сессию.

9. **Аудит соответствия принципам агентной архитектуры**: Ревьюить текущую архитектуру c0wrk относительно ключевых принципов из [Agent Architecture](https://agent-axiom.github.io/agent-arch/book/plan/). Идентифицировать пробелы: отсутствующие circuit breaker'ы, недостаточная изоляция состояния, неоптимальные паттерны восстановления после сбоев. Результат -- список конкретных доработок с приоритетами.
